# -*- Python -*-

# This file allows users to configure test-specific settings without modifying
# the main lit.cfg.py file. It is loaded by lit.cfg.py.

# Example: Override the path to clang for local testing
# config.substitutions.append(('%clang', '/path/to/local/clang'))

# Example: Skip specific tests based on platform
# import platform
# if platform.system() == 'Darwin':
#     config.unsupported = ['test/path/to/macos/unsupported/test.test']